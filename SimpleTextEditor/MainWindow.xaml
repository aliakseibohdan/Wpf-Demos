<Window x:Class="SimpleTextEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Simple Text Editor" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Icon="Icons/app_icon.ico">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Load" Click="LoadButton_Click" Margin="0,0,5,0"/>
            <Button Content="Save" Click="SaveButton_Click" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- Font Controls -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Font Family:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Grid.Column="1" x:Name="FontFamilyComboBox" SelectionChanged="FontFamilyComboBox_SelectionChanged"/>

                <Label Grid.Column="2" Content="Font Size:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <ComboBox Grid.Column="3" x:Name="FontSizeComboBox" SelectionChanged="FontSizeComboBox_SelectionChanged"
                          IsEditable="True" Text="12"/>

                <CheckBox Grid.Column="4" x:Name="BoldCheckBox" Content="Bold" VerticalAlignment="Center" 
                         Margin="10,0,5,0" Checked="BoldCheckBox_Changed" Unchecked="BoldCheckBox_Changed"/>

                <CheckBox Grid.Column="5" x:Name="ItalicCheckBox" Content="Italic" VerticalAlignment="Center" 
                         Margin="5,0,0,0" Checked="ItalicCheckBox_Changed" Unchecked="ItalicCheckBox_Changed"/>
            </Grid>
        </Border>

        <!-- Text Editor -->
        <TextBox Grid.Row="2" x:Name="EditorTextBox" 
                 AcceptsReturn="True" 
                 AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Segoe UI"
                 FontSize="12"
                 BorderThickness="1"
                 BorderBrush="#CCCCCC"
                 Padding="10"/>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Background="#E1E1E1" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>